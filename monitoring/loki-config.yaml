limits_config:
  retention_period: 72h # ◄ Automatically purge logs older than 3 days

compactor:
  working_directory: /loki/retention
  compaction_interval: 10m
  retention_enabled: true
  retention_delete_delay: 2h
